# Team Portal 环境变量
# 复制为 .env 并填入真实值，切勿提交 .env 到 Git
# 生产部署必读: JWT__KEY 必须替换为随机串，否则每次重启后端所有登录会失效

# ── 域名（生产环境 HTTPS 必需）──
DOMAIN=team.yourdomain.com
CERTBOT_EMAIL=admin@yourdomain.com

# ── CORS 允许的域名（分号分隔）──
CORS__ORIGINS=https://team.yourdomain.com

# ── 可信反向代理网段（审计 IP / 登录限流依赖它）──
# 反向代理(nginx)之后的真实客户端 IP 只在 X-Forwarded-For 里。不配置（或网段没覆盖到代理）
# 时后端会忽略该头，表现为「操作日志里所有来源 IP 都是同一个」（那是代理容器的 IP），
# 同时登录限流会把所有用户算在同一个 IP 上。
# docker compose 默认网段可用下面命令查看：docker network inspect team-portal_default | grep Subnet
# 留空/删除本行则使用 compose 内置默认 172.16.0.0/12;10.0.0.0/8;192.168.0.0/16;127.0.0.1
# （够用但偏宽：私网内的机器可以伪造该头）。要更严格就填实际网段，如 10.255.1.0/24;127.0.0.1
FORWARDEDHEADERS__KNOWNNETWORKS=

# ── JWT 密钥（至少 32 字符，生产必改!生成示例: openssl rand -base64 48）──
JWT__KEY=change-me-to-a-random-string-at-least-32-chars

# ── 管理员初始账号（仅首次建库时生效）──
# 留空则跳过创建种子管理员（日志会提示）;deploy/quickstart.sh 会自动填入随机密码。
# 切勿使用 change-me / admin123 这类占位口令直接上线。
ADMIN__USERNAME=admin
ADMIN__PASSWORD=

# ── DeepSeek AI ──
# 同时注入 backend(AISERVICE__*)与 ai-service(DEEPSEEK_API_KEY，compose 已做变量映射)
AISERVICE__DEEPSEEKKEY=sk-your-deepseek-key
AISERVICE__DEEPSEEKBASEURL=https://api.deepseek.com

# ── 百度网盘 OAuth（云备份，可留空则跳过云上传）──
BAIDU__APPKEY=your-baidu-app-key
BAIDU__SECRETKEY=your-baidu-secret-key
BAIDU__SIGNKEY=your-baidu-sign-key
